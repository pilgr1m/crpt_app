export type allTimeHighType = {
  price: string
  timestamp: number
}
export type supplyType = {
  confirmed: boolean
  total: string
  circulating: string
}
export type linksType = {
  name: string
  type: string
  url: string
}

export type statsType = {
  '24hVolume': string
  total: number
}
